# FX International CRM Demo

客户可点演示原型（mock 数据，无后端）。GitHub Pages 托管静态导出。

- 演示账号密码均为 `demo`：`advisor@demo` / `wang@demo` / `owner@demo` / `finance@demo`
- 本地开发：`npm run dev`（http://localhost:3000，不要设 `GITHUB_PAGES`）
- Pages 构建：`GITHUB_PAGES=1 npm run build`，产物在 `out/`
